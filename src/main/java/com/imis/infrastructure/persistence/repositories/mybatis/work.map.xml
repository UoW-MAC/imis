<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.imis.domain.repositories.WorkRepository">
	<cache eviction="FIFO" flushInterval="60000" size="512" readOnly="true" />
 <insert id="addWorkInfo" parameterType="java.util.List" useGeneratedKeys="false" > 
    insert into work(work_id,organization,dates,title,duties,student_id,create_time,update_time) 
    values
    <foreach collection="list" index="index" item="item"
             separator="," open="(" close=")">
              SEQ_WORK.NEXTVAL,#{item.organization},#{item.dates},#{item.title},#{item.duties},#{item.studentId},#{item.createTime},#{item.updateTime}
    </foreach>
 </insert> 
   
</mapper>

